/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BlogPipe } from './blog.pipe';

describe('Pipe: Blog', () => {
  it('create an instance', () => {
    let pipe = new BlogPipe();
    expect(pipe).toBeTruthy();
  });
});
